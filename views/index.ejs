<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Game - Play Classic Games</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
    <header class="header">
        <div class="container header-container">
            <div class="logo">
                <img src="/images/logo.png" alt="Game Play Logo"> Game Play
            </div>
            <nav class="nav">
                <ul class="nav-links">
                    <li><a href="/">Home</a></li>
                    <li><a href="/games">Games</a></li>
                    
                </ul>
                <div class="auth-status" id="auth-status">
                    <!-- Auth status will be populated by JavaScript -->
                </div>
                <button id="theme-toggle" class="btn btn-outline">
                    <!-- Theme toggle text will be set by JavaScript -->
                </button>
            </nav>
        </div>
    </header>

    <main class="main-content">
        <div class="container">
            <section class="hero">
                <h1 class="text-center mb-3">Welcome to Game Play</h1>
                <p class="text-center mb-4">Play classic games online for free. Challenge yourself and beat your high score!</p>
                
                <div class="auth-hidden">
                    <div class="text-center mb-4">
                        <a href="/signin" class="btn btn-primary">Sign In to Play</a>
                        <a href="/signup" class="btn btn-outline">Create Account</a>
                    </div>
                </div>
            </section>

            <section class="featured-games">
                <h2 class="text-center mb-4">Featured Games</h2>
                
                <div class="games-grid">
                    <% games.forEach(game => { %>
                        <div class="card">
                            <img src="<%= game.image || '/placeholder.svg?height=200&width=300' %>" alt="<%= game.title %>" class="card-img">
                            <div class="card-body">
                                <h3 class="card-title"><%= game.title %></h3>
                                <p class="card-text">
                                    <%= game.description || 'Play this classic game and challenge yourself!' %>
                                </p>
                                <a href="<%= game.path %>" class="btn btn-primary">Play Now</a>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </section>
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; <%= new Date().getFullYear() %> Game Portal. All rights reserved.</p>
        </div>
    </footer>

    <script src="/index.js"></script>
</body>
</html>

